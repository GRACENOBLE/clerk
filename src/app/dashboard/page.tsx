import Container from "@/components/common/container";

const page = () => {
  return <Container className="min-h-[80vh] grid place-items-center">This is the Dashboard</Container>;
};

export default page;
